Names: Dan 
       Alex 

Packages: bokeh, numpy, pandas, info_gain, pyitlib, sklearn, dill

**All packages are installed within the code if needed.

CLI.py imports Funcs.py and uses its functions.
